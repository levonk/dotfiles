# Minimum version: POSIX sh (portable). No reliable version check available.
# Validate presence of basic POSIX utilities used by scripts.
if ! command -v printf >/dev/null 2>&1; then
  echo "ERROR: POSIX 'printf' required." >&2
  return 1 2>/dev/null || exit 1
fi
