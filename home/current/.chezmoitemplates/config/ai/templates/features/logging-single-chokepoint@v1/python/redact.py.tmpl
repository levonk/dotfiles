# app/infra/logging/redact.py
# Generated by feature: logging-single-chokepoint@v1

from typing import Dict, Any, Iterable

def redact(obj: Dict[str, Any], keys: Iterable[str] = ('password','token','secret')) -> Dict[str, Any]:
    copy = dict(obj)
    for k in keys:
        if k in copy:
            copy[k] = '[REDACTED]'
    return copy
