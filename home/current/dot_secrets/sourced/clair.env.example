#!/usr/bin/env bash
# {{ template "dot_config/ai/templates/shell/header-comment.tmpl" . }}
#
# Clair Docker Security Scanner - Secrets Configuration
# ======================================================
#
# This file contains ONLY sensitive credentials for Clair.
# All other configuration is defined in the docker-security-check script.
#
# SECURITY NOTICE:
# - This file contains sensitive credentials
# - Permissions are enforced at 600 (owner read/write only)
# - Never commit this file to version control
# - Generate strong passwords: openssl rand -base64 32
#
# USAGE:
# 1. Uncomment and set the password below
# 2. Optionally override username if needed
# 3. Run: docker-security-check

# =============================================================================
# Secrets (REQUIRED)
# =============================================================================

# PostgreSQL database password for Clair
# Generate with: openssl rand -base64 32
#CLAIR_DB_PASSWORD="CHANGE_THIS_TO_A_SECURE_PASSWORD"

# =============================================================================
# Optional Overrides
# =============================================================================
# Only uncomment these if you need to override the script defaults

# PostgreSQL username (default: clairuser)
#CLAIR_DB_USER="clairuser"

# PostgreSQL database name (default: clair)
#CLAIR_DB_NAME="clair"

# Clair API port (default: 6060)
#CLAIR_API_PORT="6060"
